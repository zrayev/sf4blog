{% extends 'base.html.twig' %}
{% block title %}
  {{ 'Create tag'|trans }}
{% endblock %}
{% block content %}
  <h1 class="text-center">{{ 'Create tag'|trans }}</h1>
    {{ parent() }}
    {{ form_start(form, {'attr': {'novalidate': 'novalidate', 'class':'tag-form'}}) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}
